[alias]
  s = "status -sb"
  l = log --graph --all --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --date=relative
  gl = log --oneline --decorate --stat --graph
  co = checkout
  cot = checkout --track
  sf = submodule foreach
  c  = "!git log --oneline | wc -l "
  ignored = "!git ls-files --others -i --exclude-standard --directory"
  pushsub = submodule foreach "git push origin master"
  pullsub = submodule foreach "git pull origin master"
[core]
  legacyheaders = false
	excludesfile = /Users/allen.hsu/.gitexclude
  whitespace = trailing-space,space-before-tab
  quotepath = false
  autocrlf = false
  safecrlf = true
  editor = nvim
[color]
  ui = auto
[branch]
  autosetupmerge = true
[advice]
  pushNonFastForward = false
    statusHints = false
[push]
    default = current
[rerere]
  enabled = true
[difftool "vimdiff"]
	cmd = vimdiff
[mergetool "vimdiff"]
    cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
    trustExitCode = true
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
[user]
	email = 0allen.hsu@gmail.com
	name = Allen Hsu
[rebase]
	autosquash = true
[commit]
	template = /Users/allen.hsu/.stCommitMsg
[status]
	showUntrackedFiles = all
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
